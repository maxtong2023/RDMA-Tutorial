# Edit host prefix/range to match `hostname` on each machine (see: uname -n).
# Example: inf1 and inf2 -> "inf1..2" produces hostnames inf1, inf2.
servers:
	inf1..1
clients:
	inf2..2
msg_size:
	64
num_concurr_msgs:
	1
